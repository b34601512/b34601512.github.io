# 压缩包文件
*.zip
*.rar
*.7z
*.tar
*.tar.gz
*.gz
*.bz2
*.xz

# 可执行文件
*.exe
*.msi
*.dmg
*.pkg
*.deb
*.rpm
*.app

# 打包产物目录
dist/
build/
out/
release/
packages/

# 临时文件和缓存
*.tmp
*.temp
*.cache
.DS_Store
Thumbs.db
__pycache__/

# 工具与本地工作目录（不要提交）
.claude/
.pi/
.imgtest/
.shots/
# IDE 配置文件（可选，根据团队需要）
.vscode/
.idea/
*.swp
*.swo
*~
# 根目录的点文件一律不提交：探针脚本（.perf-*、.live-*）、抓取输出（.live1.out、.r-a-500.out……）、
# 线上镜像目录（.live-site/）这些都是跑完就扔的东西，曾经三次被 git add -A 顺手提交上去（Pages 会公开）。
# 只留 .gitignore 本身；以后真要提交某个点文件，在这里加一条 !否定规则。
.*
!.gitignore
